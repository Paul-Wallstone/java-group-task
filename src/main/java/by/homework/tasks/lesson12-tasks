Задание 1: Сортировка объектов с использованием Comparator
Создайте класс Student с полями name (тип String), age (тип int) и grade (тип double).
Напишите компаратор для сортировки списка студентов (LinkedList<Student>) по следующим критериям:

По имени (в алфавитном порядке).
По возрасту (от младшего к старшему).
По grade (от большего к меньшему).
Задачи:

Реализуйте три отдельных компаратора для каждого критерия.
Сортируйте список студентов по каждому из критериев и выводите результаты.

Задание 2: Проверка симметрии строки с помощью Deque
Напишите программу, которая проверяет, является ли данная строка палиндромом (симметричной) с использованием Deque (реализация LinkedList).

Задача:

Вставьте все символы строки в Deque.
Сравнивайте элементы с начала и конца, пока не дойдёте до середины. Если все символы совпадают, строка является палиндромом.
Реализуйте метод boolean isPalindrome(String input).
Пример:

Вход: "racecar" → Выход: true
Вход: "hello" → Выход: false

Задание 3: Обратный порядок элементов в очереди
Создайте программу, которая принимает последовательность чисел, добавляет их в Queue, а затем извлекает и выводит элементы в обратном порядке.

Задача:

Добавьте элементы в Queue.
Извлеките элементы из очереди и добавьте их в Deque.
Извлеките элементы из Deque и выведите их на экран.